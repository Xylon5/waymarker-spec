# Type Convention

Format:
<layer>.<namespace>.<name>[.vN]

Examples:
- fact.drive.segment.highway
- interp.drive.style.aggressive
- narr.chapter.drive_segment

Version suffix `.vN` only for breaking semantic changes.
